const DisplayFavorites = (props) => {
    return ( 
        <div>
            {props.parentNPSDetail.map((nps, index) =>{
                
            })}
        </div>
     );
}
 
export default DisplayFavorites;